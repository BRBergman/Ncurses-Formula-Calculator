void invert(int &val)
{
    
	int temp= val;
	temp = (temp*-1 );

	val = temp;
	return;
}

void invert(float &val)
{
	double temp= val;
	temp = (temp*-1 );

	val = temp;
	return;
}

void invert(double &val)
{
	double temp= val;
	temp = (temp*-1 );

	val = temp;
	return;
}
